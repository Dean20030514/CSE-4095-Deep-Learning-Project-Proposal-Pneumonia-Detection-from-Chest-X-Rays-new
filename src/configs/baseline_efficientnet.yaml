# Baseline EfficientNet - Efficient baseline
# Auto-generated configuration file

pretrained: true
data_root: data
img_size: 224
batch_size: 48
num_workers: 12
epochs: 100
lr: 0.001
weight_decay: 0.0001
early_stopping:
  patience: 20
loss: cross_entropy
use_weighted_sampler: false
augmentation:
  horizontal_flip: 0.5
  rotation_degrees: 10
  brightness: 0.1
  contrast: 0.1
optimizer: adamw
scheduler: cosine
warmup_epochs: 2
amp: true
save_best_only: false
seed: 42
model: efficientnet_b0
output_dir: runs/baseline_efficientnet

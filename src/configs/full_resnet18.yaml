# Full ResNet18 - Complete training
# Auto-generated configuration file

pretrained: true
data_root: data
img_size: 224
batch_size: 48
num_workers: 12
epochs: 100
lr: 0.0005
weight_decay: 0.0001
early_stopping:
  patience: 20
loss: focal
focal_gamma: 1.5
use_weighted_sampler: true
augmentation:
  horizontal_flip: 0.5
  rotation_degrees: 10
  brightness: 0.1
  contrast: 0.1
optimizer: adamw
scheduler: cosine
warmup_epochs: 2
amp: true
save_best_only: false
seed: 42
model: resnet18
output_dir: runs/full_resnet18
